upstream biomed-backend {
    server biomed-backend:8088;
    keepalive 64;
}

limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=api:10m rate=100r/s;

# HTTP — used for LAN / WSL2 / initial setup
server {
    listen 80;
    server_name _;

    client_max_body_size 256m;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/xml
        image/svg+xml;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
    proxy_connect_timeout 10s;
    proxy_send_timeout 60s;

    location /api/auth/login {
        limit_req zone=login burst=3 nodelay;
        proxy_pass http://biomed-backend;
    }

    location /api/ {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://biomed-backend;
    }

    location /api/actuator/ {
        allow 127.0.0.1;
        allow 172.0.0.0/8;
        deny all;
        proxy_pass http://biomed-backend;
    }

    location / {
        proxy_pass http://biomed-backend;
    }

    location ~ /\.(env|git|svn) {
        deny all;
        return 404;
    }

    access_log /var/log/nginx/biomed-access.log;
    error_log /var/log/nginx/biomed-error.log warn;
}

# HTTPS — enabled when SSL certs are mounted (cloud deployment)
#server {
#    listen 443 ssl http2;
#    server_name your-domain.com;
#
#    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#    ssl_protocols TLSv1.2 TLSv1.3;
#    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#    ssl_prefer_server_ciphers on;
#    ssl_session_cache shared:SSL:10m;
#    ssl_session_timeout 10m;
#
#    add_header Strict-Transport-Security "max-age=63072000" always;
#
#    client_max_body_size 256m;
#    gzip on;
#    gzip_vary on;
#    gzip_proxied any;
#    gzip_comp_level 6;
#    gzip_min_length 256;
#    gzip_types text/plain text/css text/javascript application/javascript application/json application/xml image/svg+xml;
#
#    add_header X-Frame-Options "SAMEORIGIN" always;
#    add_header X-Content-Type-Options "nosniff" always;
#    add_header X-XSS-Protection "1; mode=block" always;
#    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
#
#    proxy_http_version 1.1;
#    proxy_set_header Host $host;
#    proxy_set_header X-Real-IP $remote_addr;
#    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#    proxy_set_header X-Forwarded-Proto $scheme;
#    proxy_read_timeout 60s;
#    proxy_connect_timeout 10s;
#    proxy_send_timeout 60s;
#
#    location /api/auth/login {
#        limit_req zone=login burst=3 nodelay;
#        proxy_pass http://biomed-backend;
#    }
#    location /api/ {
#        limit_req zone=api burst=50 nodelay;
#        proxy_pass http://biomed-backend;
#    }
#    location /api/actuator/ {
#        allow 127.0.0.1;
#        allow 172.0.0.0/8;
#        deny all;
#        proxy_pass http://biomed-backend;
#    }
#    location / {
#        proxy_pass http://biomed-backend;
#    }
#    location ~ /\.(env|git|svn) {
#        deny all;
#        return 404;
#    }
#    access_log /var/log/nginx/biomed-access.log;
#    error_log /var/log/nginx/biomed-error.log warn;
#}
#
## Redirect HTTP to HTTPS for cloud domain
#server {
#    listen 80;
#    server_name your-domain.com;
#    return 301 https://$host$request_uri;
#}
